import React from 'react';
import TicketList from '../../Components/ticket/TicketList';

const Tickets = () => {
    return (
        <div>
            <TicketList/>
        </div>
    );
};

export default Tickets;